{
  "name": "opendb-feed",
  "version": "2.0.0",
  "description": "RSS feed generator for OpenBD book database with Vercel Functions",
  "main": "api/create-feed.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site - no build required'",
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "rss": "^1.2.2"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  }
}